# serghei-swe-re-swmm 管网初始条件与出口边界说明

本文档说明如何在 `Drainage.inp` 中设置管网**初始条件**与**出口边界条件**，以及模型内部的初始化逻辑。

---

## 1. 输入文件概览

管网相关参数均在算例目录下的 `Drainage.inp` 中配置，主要涉及以下节段：

| 节段 | 作用 |
|------|------|
| `[JUNCTIONS]` | 节点几何、**节点初始水深**、出水口节点定义 |
| `[CONDUITS]` | 管道几何、**管道初始流量** |
| `[OUTFALLS]` | 出水口边界类型、定水位/时变水位、拍门 |
| `[RIVERSTAGE]` | 时变水位边界的水位时间序列（配合 `TIMESERIES` 出水口） |

---

## 2. 初始条件

### 2.1 节点初始水深

在 `[JUNCTIONS]`中设置，通过改变`InitDepth`属性值实现：
```
[JUNCTIONS]
;;Name	Elevation	MaxDepth	InitDepth	SurDepth	pondedArea	Xcoor	Ycoor	type	sealed
0       8.0     2.0       1.20      0.0   10.0        100.0  0.0   junction  1
```

- **单位**：m
- **默认值**：`0` 表示未显式指定，由初始化逻辑自动推算，见`3. 初始化逻辑`
- **判断规则**：仅当 `InitDepth > 0` 时视为用户已指定；`InitDepth = 0` 与"未设置"等价，会参与自动推算

> 若 `InitDepth` 超过 `最大水深 + 超高`，模型会输出警告，但仍继续计算。

### 2.2 管道初始流量（InitFlow）

在 `[CONDUITS]` 中设置，通过改变`InitFlow`属性值实现：

```
[CONDUITS]
;;Name	From Node	To Node	 Length	 Roughness	InOffset	OutOffset	InitFlow	MaxFlow 	Geom1
0    0   1   200.0  0.013  0.0  0.0   0.1    0.0     0.80
```

- **单位**：m³/s
- **默认值**：`0` 表示管道初始无流量，水深由两端节点推算，

---

## 3. 初始化逻辑
### 3.1 管道初始化（link_initState）

对每个管道：

1. 将初始流量设为 `InitFlow`（`q0`）
2. 若 `InitFlow ≠ 0`，根据曼宁公式反算该流量下的**正常水深**（`link_getYnorm`），作为管道初始水深
3. 若 `InitFlow = 0`，管道初始水深暂为 0，留待`3.3`处理

### 3.2 节点水深推算（initNodeDepths）

对每个**非出水口（type为junction）**节点：

| 条件 | 处理方式 |
|------|----------|
| `InitDepth > 0` | **保留用户设定值**，不被覆盖 |
| `InitDepth = 0`，且相连管道已有初始水深 | 取所有相连管道在节点处水深的**算术平均**（含进口/出口偏移） |
| `InitDepth = 0`，相连管道初始水深均为 0 | 节点水深保持 0 |

对每个**FREE 出水口**（非 FIXED / TIMESERIES）：

- 根据相连管道的 `InitFlow`（即 `q0`）计算正常水深 `yNorm` 与临界水深 `yCrit`
- 出水口水深取 `min(yNorm, yCrit)`（若管道出口有高程偏移 `offset > 0`，则水深置 0）
- 当 `InitFlow = 0` 时，出水口水深为 0

> **FIXED / TIMESERIES 出水口**在此步骤中跳过，由后续边界步骤直接赋水位。

### 3.3 管道水深补充（initLinkDepths）

对每个 `InitFlow = 0` 的管道：

- 取上下游节点初始水深（扣除各自偏移量），求算术平均，作为管道初始水深
- 结果限制在 `[FUDGE, yFull]` 范围内

对每个 `InitFlow ≠ 0` 的管道：

- **不再修改**管道水深，保持步骤 3.1 中由曼宁均匀流反算得到的正常水深
- **不再考虑**两端节点的 `InitDepth`

### 3.4 定水位 / 时变水位边界覆盖（setOutfallBoundaryDepths）

在节点与管道水深均确定后，对 FIXED / TIMESERIES 出水口：

```
节点水深 = max(0, 边界水位绝对高程 − 节点底高程)
```

此步骤**直接使用边界水位**，**不考虑**相连管道的 `InitFlow`。

## 4. 出口边界条件

出口边界在 `[OUTFALLS]` 节段配置。出水口节点须在 `[JUNCTIONS]` 中将类型设为 `outlet`。

### 4.1 格式说明

```
[OUTFALLS]
;; nodeId  mode  param  gated
```

| 字段 | 含义 |
|------|------|
| `nodeId` | 出水口节点编号（与 `[JUNCTIONS]` 中 `outlet` 节点 id 对应） |
| `mode` | 边界类型：`FREE` / `FIXED` / `TIMESERIES`（大小写不敏感） |
| `param` | 模式相关参数（见下表） |
| `gated` | 是否安装拍门：`yes`/`no`（或 `1`/`0`、`true`/`false`） |

### 4.2 三种边界类型

#### （1）FREE — 自由出水口

```
[OUTFALLS]
;; nodeId  mode  param  gated
1  FREE  0  no
```

- `param` 无实际意义
- 节点水深由相连管道的流量决定：取正常水深与临界水深的较小值
- 初始化时以 `InitFlow` 作为流量；计算过程中以当前管道流量实时更新
- 适用于河道水位较低、不发生顶托的排水出口

#### （2）FIXED — 定水位边界

```
[OUTFALLS]
;; nodeId  mode  param  gated
1  FIXED  8.5  no
```

- `param` 为**绝对水位高程**（m），非相对水深
- 节点水深 = `max(0, param − 节点底高程)`
- 适用于下游水位恒定（湖泊、水库、稳定河道）的情形

#### （3）TIMESERIES — 水位随时间变化

```
[OUTFALLS]
;; nodeId  mode  param  gated
1  TIMESERIES  0  yes
```

- `param` 为 `[RIVERSTAGE]` 中的序列编号 `seriesId`
- 须在 `[RIVERSTAGE]` 中提供对应时间序列：

```
[RIVERSTAGE]
;; 列: seriesId  时间(s)  水位(m, 绝对高程)
0       0         8.5
0       200       8.5
0       400       9.5
0       600       7.0
```

- 模型按当前模拟时刻对序列做线性插值，得到边界水位
- 节点水深 = `max(0, 插值水位 − 节点底高程)`
- 初始化时取 `t = 0` 时刻的序列值，**忽略** `InitFlow`
- 适用于潮汐、涨落河、水库调度等时变下游水位

### 4.3 拍门（gated）

`gated` 控制出水口是否安装拍门（止回阀，防止河水倒灌）：

| 设置 | 含义 | 作用 |
|------|------|------|
| `gated = yes` | 有拍门 | 当水流方向为**倒灌**（计算出的流量方向是从出口向管网内部流动）时，将该管段流量强制为 0，禁止水体倒灌 |
| `gated = no` | 无拍门 | 允许倒灌，高水位下游可顶托管网 |

拍门在**每个时间步**的流量计算中生效（`link_setFlapGate`），与边界类型独立——FIXED / TIMESERIES / FREE 均可配置拍门。

> **典型场景**：模拟河道高水位顶托倒灌时，须设 `gated = no`；若需模拟拍门阻挡倒灌，设 `gated = yes`。

---

## 5. 配置示例

### 5.1 两个节点（N0、N1），一根管道（C0），N0初始水深为1.2m，C0无初始流量（管道初始水深会由0号节点初始水深推算），出水口边界条件为自由出流，无拍门。

```
[JUNCTIONS]
0  8.0  2.0  1.20  0.0  10.0  100.0  0.0  junction  1
1  7.0  3.0  0.00  0.0  100.0  300.0  0.0  outlet    1

[CONDUITS]
0  0  1  200.0  0.013  0.0  0.0  0.0  0.0  0.80

[OUTFALLS]
1  FREE  0  no
```

### 5.2 时变水位出口（TIMESERIES）+ 拍门

```ini
[JUNCTIONS]
0  8.0  2.0  1.20  0.0  10.0  100.0  0.0  junction  1
1  7.0  3.0  0.00  0.0  100.0  300.0  0.0  outlet    1

[CONDUITS]
0  0  1  200.0  0.013  0.0  0.0  0.0  0.0  0.80

[OUTFALLS]
1  TIMESERIES  0  yes

[RIVERSTAGE]
0  0    8.5
0  200  8.5
0  400  9.5
0  600  7.0
```

- `t = 0` 时边界水位 8.5 m；N1 底高程 7.0 m → **初始水深 1.5 m**
- `gated = yes`：河水倒灌时被拍门阻断
- `gated = no`：允许高河位时水体倒灌进入管网

### 5.3 管道有初始流量

```ini
[JUNCTIONS]
0  8.0  2.0  1.20  0.0  10.0  100.0  0.0  junction  1
1  7.0  3.0  0.00  0.0  100.0  300.0  0.0  outlet    1

[CONDUITS]
0  0  1  200.0  0.013  0.0  0.0  0.50  0.0  0.80

[OUTFALLS]
1  FREE  0  no
```

- C0 初始水深由 `q0 = 0.50 m³/s` 经曼宁公式反算，**不受** N0、N1 的 `InitDepth` 影响
- 若 N1 为 FREE 出水口且未设 `InitDepth`，N1 水深取 `min(yNorm, yCrit)`，对应 `q0 = 0.50 m³/s`

---

## 6. 配置要点与常见误区
### 6.1 水位单位

- `FIXED` 的 `param`、`[RIVERSTAGE]` 的水位：**绝对高程**（m），与节点底高程同一高程基准

### 6.2 sealed 参数

`[JUNCTIONS]` 最后一列 `sealed`：

- `sealed = 1`：关闭该节点与 2D 地表的水力交换，仅由 1D 管网控制节点水深（验证初始条件时推荐使用）
- `sealed = 0`：允许 1D–2D 耦合交换



