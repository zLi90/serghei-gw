# 泵站配置说明

## 1. 输入文件概览

| 节段 | 作用 |
|------|------|
| `[PUMPS]` | 泵链接（from → to）、曲线编号、初始开关、启停水深 |
| `[CURVES]` | 泵的扬程–流量（H–Q）曲线点列 |
| `[STORAGE]` | 蓄水池（可选，FUNCTIONAL / CYLINDRICAL） |
| `[JUNCTIONS]` | 普通节点、出水口节点 |
| `[CONDUITS]` | 重力管道 |
| `[OUTFALLS]` | 出水口边界（FREE / FIXED / TIMESERIES） |
| `[RIVERSTAGE]` | 时变水位序列（配合 `TIMESERIES` 出水口） |

泵在模型中是作为连接泵前节点和泵后节点的**link**，与 `[CONDUITS]` 同级，只不过需要在 `[PUMPS]` 中定义。

---

## 2. 支持的泵类型

当前支持两种泵类型：

| 类型 | inp 指定方式 | 流量如何确定 |
|------|--------------|--------------|
| **扬程-流量** | `[PUMPS]` 中 `curveId` 为整数，并在 `[CURVES]` 中提供 `扬程-流量H-Q` 对应表格 | 由实时**扬程 H** 在 H–Q 曲线上**分段线性插值**得 Q |
| **理想泵站** | `[PUMPS]` 中 `curveId` 为 `*`、`IDEAL` 或 `-1` | 流量为泵前节点来流，来多少泵走多少，不看扬程 |


---

## 3. `[PUMPS]` 节段

### 格式说明

```
[PUMPS]
;; id  from  to  curveId  initOn  yOn  yOff
```

| 字段 | 含义 |
|------|------|
| `id` | 泵链接编号|
| `from` | 泵前节点编号（泵前节点，可为JUNCTION节点或蓄水池STORAGE节点） |
| `to` | 泵后节点编号（可为JUNCTION节点，表示排水系统中段加泵站，或者是出水口outfall，表示泵站抽出的水直接排出管网系统） |
| `curveId` | 扬程-流量型泵站：与 `[CURVES]` 第一列一致；理想泵站：`*` / `IDEAL` / `-1` |
| `initOn` | 初始开关：`1` 开，`0` 关（默认 `1`） |
| `yOn` | **启泵水深**（m），泵前节点**水深**；`0` 表示不启用启泵控制 |
| `yOff` | **停泵水深**（m），必须 **< yOn** |

启停仅依据**泵前节点水深**，与泵后节点（或下游河位）无直接关系；下游高水位通过改变扬程 H 影响扬程-流量型泵站的流量大小。

---

## 4. `[CURVES]` 与扬程–流量曲线

### 格式说明

```
[CURVES]
;; curveId  type   H(m)   Q(m³/s)
0          pump3  0.0    0.50
0          pump3  1.5    0.40
0          pump3  3.0    0.30
0          pump3  5.0    0.000
```

| 列 | 含义 | 单位 |
|----|------|------|
| 第 1 列 | 曲线编号，与 `[PUMPS]` 的 `curveId` 对应 | — |
| 第 2 列 | 固定写 `pump3` 或 `PUMP3` | — |
| 第 3 列 | 扬程 **H** | **m** |
| 第 4 列 | 流量 **Q** | **m³/s** |

- 曲线为**分段线性表**：相邻两点之间线性插值；`H` 低于首点取首点 `Q`，高于末点取末点 `Q`。
- 此处不采用统一的扬程-流量二次/三次公式，因为泵站型号不同，公式类型不同，无法做到统一。给水排水设计手册中提供的也是不同泵的散点格式。若泵站的扬程-流量曲线光滑，应在表中**加密控制点**以减小插值误差。

---

## 5. IDEAL理想型泵

```
[PUMPS]
0  1  2  IDEAL  1  0  0
```

- 不读 `[CURVES]`。
- 出口流量 = 泵前节点当前步来流。
- **不**根据扬程削减流量；**不**对非蓄水池泵前节点做库容限制。
- 适用于“定流量提升”“能力无限”的简化场景。

---

## 6. 泵前蓄水池与库容限制

### `[STORAGE]` 简要说明

泵前节点可设为蓄水池，蓄水池类型同样是节点，这点与检查井及雨水口一致，所以id需要与JUNCTION保持一致，详细参考配置示例。SWMM中涉及不规则蓄水池，serghei目前考虑两种规则蓄水池：棱柱体和圆柱体蓄水池。需要在 `[STORAGE]` 中单独定义，参数用来根据蓄水池水深D计算面积A。

说明：蓄水池不是必须和泵站绑定，蓄水池本身就是排水系统包含的一个组件，放在哪里都可以。

| 形状 | inp 关键字 | 说明 |
|------|------------|------|
| 棱柱 / 一般曲线 | `FUNCTIONAL` | `p1=a1, p2=a2, p3=a0`；体积 V(d) 由 a0,a1,a2 积分，计算公式为 𝐴 = 𝑎0 + 𝑎1𝐷^𝑎2|
| 圆柱 | `CYLINDRICAL` | `p1=长, p2=宽`（直径方向尺寸），内部转为底面积 π(L/2)(W/2) |

以下表示一个底部高程为5m，最高可以积3m水深，底面积是25平米的棱柱形蓄水池：
```
[STORAGE]
;; id  Elev  MaxDepth  InitDepth  Shape  p1  p2  p3  SurDepth  PondedArea  X  Y
1     5.0   3.0       0.0        FUNCTIONAL  25.0  0.0  0.0  0.0  0.0  150.0  0.0
```
---

## 7. 配置示例

### 7.1 拓扑: `N0` —`泵站P0`→ `N1` —`普通管道C0`→ `N2(FREE 出水口)`

```
[JUNCTIONS]
;;Name	Elevation	MaxDepth	InitDepth	SurDepth	pondedArea	Xcoor	Ycoor	type	sealed
0  7.0  2.0  0.0  0.0  300.0   20.0  0.0  junction  0  
1  7.0  2.0  0.0  0.0   10.0  100.0  0.0  junction  0   
2  6.0  3.0  0.0  0.0  100.0  200.0  0.0  outlet    0 

[PUMPS]
;; id  from  to  curveId  initOn  yOn  yOff
0  0  1  0  0  0.8  0.2          

[CURVES]
;; curveId  type   H(m)   Q(m³/s)
0  pump3  0.0  0.100
0  pump3  2.0  0.080
0  pump3  4.0  0.050
0  pump3  6.0  0.000

[CONDUITS]
;;Name	From Node	To Node	 Length	 Roughness	InOffset	OutOffset	InitFlow	MaxFlow 	Geom1
0  1  2  100.0  0.013  0.0  0.0  0.0  0.0  0.60    

[OUTFALLS]
;; nodeId  mode  param  gated
2  timeseries  0  yes

[RIVERSTAGE]
;; seriesId  时间(s)  水位(m, 绝对高程)
0  0     6.5
0  3600  6.5
```

- 泵前为普通 junction，库容由 `pondedArea × depth` 近似，所以可以通过将pondedArea设置为较大值来近似蓄水池（但是普通junction与蓄水池节点水深更新策略不同）。
- 出口 TIMESERIES + 拍门，可模拟河位顶托。

---

### 7.2 拓扑：`N0` —`C0`→ `N1(蓄水池)` —`P0`→ `N2(FREE 出水口)`

```
[JUNCTIONS]
0  7.0  2.0  0.0  0.0  300.0   50.0  0.0  junction  0   

[STORAGE]
;; id  Elev  MaxDepth  InitDepth  Shape  p1  p2  p3  SurDepth  PondedArea  X  Y
1  5.0  3.0  0.0  FUNCTIONAL  25.0  0.0  0.0  0.0  0.0  150.0  0.0

[JUNCTIONS]
2  6.5  2.5  0.0  0.0  100.0  250.0  0.0  outlet    1

[CONDUITS]
0  0  1  80.0  0.013  0.0  0.0  0.0  0.0  0.50

[PUMPS]
1  1  2  0  0  0.4  0.15

[CURVES]
0  pump3  0.0  0.50
0  pump3  1.5  0.40
0  pump3  3.0  0.30
0  pump3  5.0  0.000

[OUTFALLS]
2  FREE  0  no
```

---

### 7.3 在 7.2 基础上将 N2 改为时变水位，河位最高 **10 m**：

```
[OUTFALLS]
2  timeseries  0  no

[RIVERSTAGE]
0  0     6.0
0  1800  10.0
0  6000  10.0
0  7720  7.0
```

- 河位 10 m、蓄水池水深 0.4 m 时：  
  扬程`H = 10 − (5 + 0.4) = 4.6 m < 5 m` → 泵曲线仍给出 **Q > 0**，也就是说，泵站可以保证在河道水位小幅抬升时（抬升过大，超出泵站最大扬程那就泵不出去了），仍将管网系统中的水排出去。
- 泵站不允许泵后节点水倒灌入泵前节点。也就是说，当泵后接出口时，出口高水位边界不会造成倒灌。

---

